/**
 * Canonical role display labels — single source of truth.
 * Import this wherever a role needs a human-readable name instead of the DB enum.
 */
export const ROLE_LABELS = {
  ADMIN: 'Admin',
  LEADER: 'Leader',
  MEMBER: 'Member',
  SUPERVISOR: 'Supervisor',
};
