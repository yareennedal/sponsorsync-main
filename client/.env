# =============================================================================
# COMMITTED DELIBERATELY - see the header in server/.env.
# Nothing here is secret: VITE_* values are compiled into the browser bundle
# and are visible to anyone who opens the site. Never put a secret in this file.
# =============================================================================

# Backend API base URL. Used by Axios with withCredentials: true.
VITE_API_URL=http://localhost:4000
